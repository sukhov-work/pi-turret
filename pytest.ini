[pytest]
pythonpath = .
testpaths = tests
markers =
    hardware: needs a real Pi / I2C / camera — skipped on the Mac
    slow: loads a model or large fixture
    integration: multiple layers wired with fakes
addopts = -ra
