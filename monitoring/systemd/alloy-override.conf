# Deployed to /etc/systemd/system/alloy.service.d/override.conf
# Loads the Grafana Cloud credentials/endpoints into Alloy's environment so config.alloy's
# sys.env(...) lookups resolve. Keep the referenced file (/etc/alloy/secrets.env) at chmod 600.
[Service]
EnvironmentFile=/etc/alloy/secrets.env
