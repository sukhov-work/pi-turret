# pi-turret v2 — Decisions Log

One line per meaningful change: what was decided, files touched, any number measured on-device.

- **2026-06-27 — Phase 0 + Phase 1 pure-logic spine scaffolded (Mac).** Built the v2 tree at the **repo root** (top-level packages `detect/ track/ strategy/ aim/ actuate/ app/`, `config.py`, `contracts.py`, `errors.py`, `main.py`) — matches the `from detect import …` convention imports and the `rsync ./ → ~/pi-turret-v2/` deploy. Implemented + unit-tested on the Mac (venv `.venv-v2`, Python 3.9.6, **129 passed / 1 skipped**): anchor-free `decode_v8` + NMS (golden v5/v8 guard), `IouTracker` (greedy-IoU + constant-velocity, no scipy/ByteTrack), CV lead predictor, tunable scoring + hysteresis selector, affine calibration (apply+fit, v1 preset reproduced), `PIController`+`slew_toward`, kill-zone geometry, `ServoController` (clamps angle **and** pulse), non-blocking `Pump`, `FireStateMachine` (pump-off on every FIRING exit), `ControlLoop` integration, `LatestSlot`. Authored hardware/IO (lazy imports, no import side effects): `actuate/pca9685.py` (init-once port), `capture.py`, `detect/coral.py`, `app/{pipeline,annotate,snapshots}.py`, `main.py` (guarded + atexit/SIGTERM disarm). **v1 untouched.**
- **Gated / not yet done (need other machines):** model export + `edgetpu_compiler` (Strix), real-model golden fixture (`tests/fixtures/raw_output.npy` + `predict_ref.json`), all on-Pi truth (camera FPS, Coral latency, servo dry-run, calibration fit, decoy fire), and web UI (1.11) + USB streamer (1.12) + motion-gating seam (1.10).
- **Open flag — servo pulse band:** docs cite MG996R ~1000–2000 µs, but v1's *actual* operating pulses are ~556–1023 µs (slope `2000/180` + offset `501`). v2 keeps v1's verified mapping and a wide `[500, 2500] µs` hard guard; **re-measure on the Pi.**
- **2026-06-27 — Operator I/O (LCD + indicators) + IR-remote seam; docs/memory synced.** Reused v1's exact wiring (audited from `v1/TurretHandler.py:40-51` / `v1/PCA9685.py:32`): pump BCM26, aux laser BCM27, status LED BCM23, PCA9685 I2C1@0x40, **LCD on the same I2C bus 1** (`rpi_lcd`), pan ch1/tilt ch0. Added `actuate/lcd.py` (`StatusLcd`, fail-safe) + `app/display.py` (`format_lcd_lines` pure + `LcdReporter` thread) showing boot/IP→state→target→aim-err→KZ→fps→shots; `actuate/indicators.py` (status LED + **opt-in** aux marker, laser-safety default off) toggled in `ControlLoop`; `app/remote.py` IR seam (rc-core/evdev, lazy) + `RemoteConfig`. Wired in `main.py` (LAN-IP, disarm shows SAFE). **146 tests pass / 1 skipped.** Updated IMPLEMENTATION_PLAN §8 (status/wiring/steps 1.13–1.15), V2-design-plan, README, `conventions/hardware-safety.md`, SKILL.md, and Serena `mem:architecture/wiring`.
- **Owner constraint (recorded):** physical wiring is FIXED — reuse v1 pins, **escalate before any rewire**; new hardware additive on free pins only. IR receiver is PROPOSED on free **BCM17** (confirm) via `dtoverlay=gpio-ir`; capture key codes on the Pi.
