# pi-turret v2 — Decisions Log

One line per meaningful change: what was decided, files touched, any number measured on-device.

- **2026-06-27 — Phase 0 + Phase 1 pure-logic spine scaffolded (Mac).** Built the v2 tree at the **repo root** (top-level packages `detect/ track/ strategy/ aim/ actuate/ app/`, `config.py`, `contracts.py`, `errors.py`, `main.py`) — matches the `from detect import …` convention imports and the `rsync ./ → ~/pi-turret-v2/` deploy. Implemented + unit-tested on the Mac (venv `.venv-v2`, Python 3.9.6, **129 passed / 1 skipped**): anchor-free `decode_v8` + NMS (golden v5/v8 guard), `IouTracker` (greedy-IoU + constant-velocity, no scipy/ByteTrack), CV lead predictor, tunable scoring + hysteresis selector, affine calibration (apply+fit, v1 preset reproduced), `PIController`+`slew_toward`, kill-zone geometry, `ServoController` (clamps angle **and** pulse), non-blocking `Pump`, `FireStateMachine` (pump-off on every FIRING exit), `ControlLoop` integration, `LatestSlot`. Authored hardware/IO (lazy imports, no import side effects): `actuate/pca9685.py` (init-once port), `capture.py`, `detect/coral.py`, `app/{pipeline,annotate,snapshots}.py`, `main.py` (guarded + atexit/SIGTERM disarm). **v1 untouched.**
- **Gated / not yet done (need other machines):** model export + `edgetpu_compiler` (Strix), real-model golden fixture (`tests/fixtures/raw_output.npy` + `predict_ref.json`), all on-Pi truth (camera FPS, Coral latency, servo dry-run, calibration fit, decoy fire), and web UI (1.11) + USB streamer (1.12) + motion-gating seam (1.10).
- **Open flag — servo pulse band:** docs cite MG996R ~1000–2000 µs, but v1's *actual* operating pulses are ~556–1023 µs (slope `2000/180` + offset `501`). v2 keeps v1's verified mapping and a wide `[500, 2500] µs` hard guard; **re-measure on the Pi.**
- **2026-06-27 — Operator I/O (LCD + indicators) + IR-remote seam; docs/memory synced.** Reused v1's exact wiring (audited from `v1/TurretHandler.py:40-51` / `v1/PCA9685.py:32`): pump BCM26, aux laser BCM27, status LED BCM23, PCA9685 I2C1@0x40, **LCD on the same I2C bus 1** (`rpi_lcd`), pan ch1/tilt ch0. Added `actuate/lcd.py` (`StatusLcd`, fail-safe) + `app/display.py` (`format_lcd_lines` pure + `LcdReporter` thread) showing boot/IP→state→target→aim-err→KZ→fps→shots; `actuate/indicators.py` (status LED + **opt-in** aux marker, laser-safety default off) toggled in `ControlLoop`; `app/remote.py` IR seam (rc-core/evdev, lazy) + `RemoteConfig`. Wired in `main.py` (LAN-IP, disarm shows SAFE). **146 tests pass / 1 skipped.** Updated IMPLEMENTATION_PLAN §8 (status/wiring/steps 1.13–1.15), V2-design-plan, README, `conventions/hardware-safety.md`, SKILL.md, and Serena `mem:architecture/wiring`.
- **Owner constraint (recorded):** physical wiring is FIXED — reuse v1 pins, **escalate before any rewire**; new hardware additive on free pins only. IR receiver is PROPOSED on free **BCM17** (confirm) via `dtoverlay=gpio-ir`; capture key codes on the Pi.
- **2026-06-28 — Machine access (Tailscale SSH) verified + git push-to-deploy stood up; Mac = source of truth.** Reach both boxes by SSH-over-Tailscale from the Mac with the dedicated key in `.claude/.env` (gitignored). **Pi (alias `pi`) ✓** (Bullseye, Py 3.9.2, git 2.30.2, tmux + mosh-server, 98G free). **Strix (alias `strix`) GATE** — its `authorized_keys` lacks our pubkey (offered + rejected); owner must run `ssh-copy-id` (Mac key) before Strix work. Replaced the `rsync → ~/pi-turret-v2` deploy with **git push-to-deploy** to `~/pi-turret`: each box = non-bare repo + `receive.denyCurrentBranch=updateInstead`, Mac pushes `main`. **Pi initialized + pushed:** HEAD `41cdb5c`, 563 files, clean tree, `.env` confirmed **absent** on the box. Fixed a `.env` typo (Strix host was mislabeled `RASPBERRY_TAILSCALE_HOST` → `STRIX_HALO_TAILSCALE_HOST`). Prefer **mosh + tmux** for interactive/long sessions; rsync/scp reserved for artifacts only. Updated `mem:project/dev_environment` + local auto-memory. Files: `.serena/memories/project/dev_environment.md`, this log; `.claude/.env` (local, gitignored).
- **2026-06-28 — Strix authorized + repo initialized; both boxes now consistent at Mac HEAD.** Owner ran `ssh-copy-id`; `ssh strix` works (Ubuntu x86-64, **Py 3.13.7**, git 2.51, tmux + mosh, 602G free). Initialized `~/pi-turret` on Strix (non-bare + `updateInstead`), added Mac remote `strix`, pushed `main`: HEAD `770d2a3`, 563 files, clean, `.env` **absent**. Synced the Pi up to `770d2a3` too — **both boxes at Mac HEAD**. SSH aliases `pi`/`strix` added to `~/.ssh/config` (scoped; `github.com` still uses `id_ed25519`, verified via `ssh -T`). ⚠️ **`edgetpu_compiler` NOT on PATH on Strix** — must be installed before the Edge-TPU compile phase.
- **2026-06-29 — Step 1.11 web UI authored (Mac); v1 untouched.** New `app/web.py` = pure `TurretWebController` (Mac-testable) + thin Bottle adapter with **lazy `import bottle`** (Mac venv has no bottle, imports stay clean). Routes mirror v1 (`/api/cmd`, `/api/control-cmd`, `/api/turret-state`) + new `GET /api/telemetry`, `GET|POST /api/config`. Live tuning = **atomic whole-section Config swap** (control thread never reads a torn section) + `Config.validate()` with **rollback** + string→type coercion (HTML posts strings); editable sections whitelisted (hardware/driver sections excluded). Manual jog **refused unless SAFE/disarmed** (control thread is the only servo mover while armed — v1's guard). Added `ControlLoop.apply_config()` to refresh the few init-snapshotted values (calibration, selector hysteresis/dwell, aux-enable, frame dims); all other tunables already read live per tick. `app/web_ui.html` = minimal vanilla-JS console (telemetry poll @2 Hz, arm/fire/center/aux, jog pad, auto-generated per-section tuning forms). Web runs on a **daemon thread** (`cfg.app.web_port`=8001), started in `main.py`. Tests: `tests/test_web.py` (+18) → **164 passed / 1 skipped** on `.venv-v2`. **Pi-only / UNVERIFIED:** real Bottle serving, live HTTP tuning not stalling the loop, browser UX. Files: `app/web.py`, `app/web_ui.html`, `app/control.py`, `main.py`, `tests/test_web.py`, IMPLEMENTATION_PLAN §8.
- **2026-06-29 — Step 1.12 USB-webcam streamer authored (Mac); v1 untouched.** New `app/streamer.py::UsbStreamer` = a **separate mjpg-streamer subprocess** (UVC hardware-MJPEG passthrough) so the Pi spends **no detection compute** on rendering (Pi-Cam detection path is never streamed) — mirrors v1's approach as the rollback. Pure `build_argv()` (no shell → no injection) + lifecycle gating (enabled/idempotent-start/stop/`is_running`) are Mac-tested with an **injected runner**; the real `subprocess.Popen` is Pi-only. Streaming is **non-critical**: spawn/stop failures are logged + swallowed (never touches the control loop). New `StreamConfig` (device `/dev/video0`, 640×480@15, port 8080, `binary=mjpg_streamer`, `plugin_dir` defaults to v1's experimental build where `input_uvc.so`/`output_http.so` live). Web switch wired: `TurretWebController` gained `streamer=` + `stream_usb`/`stream_off` commands + a `stream{source,running,port}` telemetry block + `stream` in editable sections; `app/web_ui.html` got a Stream card (USB on/off + live `<img>` to `:port/?action=stream`). `main.py` builds the streamer, starts it when `app.stream_source=="usb"`, registers `atexit` stop. Tests: `tests/test_streamer.py` (+8) & stream cases in `tests/test_web.py` (+4) → **176 passed / 1 skipped**. **Pi-only / UNVERIFIED:** the exact mjpg-streamer binary path + device/resolution/fps the real webcam supports, and detection-FPS-unaffected while streaming. Files: `app/streamer.py`, `config.py`, `app/web.py`, `app/web_ui.html`, `main.py`, `tests/test_streamer.py`, `tests/test_web.py`, IMPLEMENTATION_PLAN §8.
- **2026-06-29 — Web UI revamped to a mil-tech fire-control console + made wifi/Tailscale-agnostic.** (1) **UI:** rewrote `app/web_ui.html` as a minimalistic FCS console per owner preference (instrument panel, not a web app / not neon) — monospace, UPPERCASE, sharp corners, phosphor-green-on-near-black (amber=aiming, red=fire, cyan=prediction). Header status strip (STATE/TRACKS/RATE/SHOTS + SYS/FIRE/LINK tags, FIRING pulses, LINK-LOST on >1.5s stall); left fire-control + manual slew (auto-disabled unless disarmed) + optics/stream; **center tactical `<canvas>`** rendering detection-frame space (grid, reticle, kill-zone [fills red on would_fire], tracks w/ #id + velocity vectors, selected highlighted, predicted-lead X) + USB visual feed; right telemetry/track-table/live-tuning. Added `frame{w,h}` + `killzone{}` to `telemetry()` for the canvas. Design spec saved as Serena `mem:patterns/ui_aesthetic`; preference saved to auto-memory `feedback_ui_aesthetic`. (2) **Tailscale/dynamic-IP:** root-caused the old "must hardcode IP" pain — it was a Pi-side **static IP**, not the app (v1's UI already used `location.hostname`). Verified on the Pi (read-only): it's **already on DHCP** (got `192.168.50.7`), Tailscale `pi-jayson` / `100.125.7.98`. v2 binds the web server to **0.0.0.0** and mjpg-streamer to all interfaces, and the stream `<img>` is **host-relative**, so LAN-dynamic-IP, Tailscale-IP, and **MagicDNS hostname** all work on any wifi with no reconfig. `main.py` now logs **every reachable endpoint** (lan/tailscale/magicdns) for web + stream at startup (`_log_reachable_endpoints`, Tailscale = 100.64/10). **No Pi network changes made.** Tests: **177 passed / 1 skipped**; UI JS `node --check` clean. **Pi-UNVERIFIED:** browser rendering + that mjpg-streamer binds the Tailscale iface (expected; default all-interfaces). Files: `app/web_ui.html`, `app/web.py`, `main.py`, `tests/test_web.py`.
- **2026-06-29 — Re-phased the plan around the detector (model) work; acknowledged the UI was built out of order.** Owner is ready to do the YOLO+SSD model work (export/compile on Strix, integrate, test); confirmed the UI steps (1.11/1.12) + IO (1.13/1.14) jumped ahead of Step 1.1, the true blocker. **Reorganized `IMPLEMENTATION_PLAN.md`:** §8 now has a "Critical path & honest status" callout, a **"Deployment reality"** table, and a **"Gates"** list; added **§9 "Detector Build Track"** — the concrete machine-tagged sequence (9.0 Strix toolchain setup → 9.1 model sourcing → 9.2 compile gate → 9.3 golden fixture → 9.4 Pi latency → 9.5 integrate → 9.6 record/choose), framing it as a **MobileDet-vs-YOLOv8n bake-off** (research: MobileDet@320 = safe default, YOLOv8n@256 = gated upgrade since its head may split to CPU, SpaghettiNet = top later candidate). **Measured state (read-only, 2026-06-29):** Strix (x86-64, Ubuntu 25.10, Py 3.13.7, **Docker 28.2.2 present**, 602 G free) has **no `edgetpu_compiler` and no `ultralytics/tensorflow/onnx2tf`** → needs a one-time isolated setup (export venv on Py 3.10/3.11 + compiler via Docker). **No `.pt` in the repo** — only pigeon YOLOv8s ONNX@384 + compiled `pigeon-y8s_edgetpu{192,384,640}.tflite` + SSD-MobileNet-V3 COCO frozen graph (`v1/models/ssd/`); recommended first-light = stock COCO `yolov8n.pt` + Coral-zoo MobileDet (filter cls 14=bird) to de-risk before training. **Deploy drift found:** v2 actually runs from the **GitHub clone** `~/opencv_test_detection/YoloRunner` (Pi, `fabfbc8`, 1 behind) — the documented push-to-deploy `~/pi-turret` is **stale** on both Pi (`770d2a3`) and Strix (`770d2a3`); the earlier `ModuleNotFoundError: app.streamer` was from that stale clone. Recommend standardizing on the GitHub hub + one canonical dir per box (owner to confirm). New Serena memory `mem:decisions/detector_build_plan`; `mem:project/dev_environment` flagged with the deploy caveat. **No code changed; no Pi/Strix mutations.** Files: `IMPLEMENTATION_PLAN.md` (§8 rewrite + §9), this log.
- **2026-06-29 — Strix model toolchain installed + model strategy locked (owner go-ahead).** Owner OK'd installing on Strix; existing models declared **junk/obsolete — training from scratch**. **Installed on Strix:** `edgetpu_compiler` **v16.0** (apt, Coral `coral-edgetpu-stable` repo, signed-by keyring, global) + a uv-managed **Python 3.12.13 venv `~/turret-ml`** (uv 0.11.25 via pipx — Strix's only system Python is 3.13, which the export chain rejects: ultralytics pins **`tensorflow<=2.19.0`** → needs Py ≤3.12; venv chosen over global for that reason) with `ultralytics roboflow onnx onnxslim pip` (TF/onnx2tf auto-installed by `yolo export`). GPU: **ROCm 7.1 + gfx1151 (Ryzen AI MAX 395)** available for optional torch-ROCm training later (CPU for now). Sudo via `.env` password piped over stdin (never logged). **Model strategy (decided):** train **YOLOv8n@256, 3-class** (pigeon/crow/magpie) from the owner's **Roboflow** set (**1000+ imgs**, export YOLOv8 format via `roboflow` pip+key) **locally on Strix**; P1 treats **any class as a fire-eligible bird** (`num_classes=3`, class-agnostic firing) — keeps species in telemetry/snapshots for the flywheel; collapse-to-1 is the fallback. The **Roboflow images double as the >300-img INT8 calib set** (solves that gate). Flow: train → export edgetpu (Stage-0 gate) → golden fixture → deploy test → collect real imgs → iterate. Ran an **end-to-end smoke test** (stock `yolov8n.pt`→edgetpu@256, coco8 calib) to validate the chain + read the YOLOv8n op-map/subgraph count (**result pending — see next entry**). Updated `IMPLEMENTATION_PLAN §9.0/§9.1` + `mem:decisions/detector_build_plan`. Files: `IMPLEMENTATION_PLAN.md`, this log; (Strix) `~/turret-ml`, `/tmp/mlbuild.log`.
- **2026-06-29 — Stage-0 compile gate PASSED on YOLOv8n@256 → YOLOv8n confirmed the primary detector (research's MobileDet-default overridden by our measured op-map).** Smoke-tested stock COCO `yolov8n.pt` → `edgetpu` @256 INT8 (coco8 calib) end-to-end on Strix (`~/turret-ml`, edgetpu_compiler 16.0). Compiler report: **`Number of Edge TPU subgraphs: 1`**, **Total ops 256 — ALL mapped to Edge TPU, 0 on CPU** (incl. SOFTMAX/TRANSPOSE/STRIDED_SLICE/RESHAPE — the exact ops the research said would split to CPU; they don't on edgetpu_compiler v16.0 + current ultralytics), **on-chip 3.57 MiB used / 3.23 MiB free**, **off-chip streaming 7.88 KiB** (≈0; vs the strict 0.00 B ideal — negligible, not the catastrophic streaming case), compiled 930 ms, artifact `yolov8n_full_integer_quant_edgetpu.tflite` (3.8 MB). Confirms the design-doc claim that v1's Coral failure was **decode, not compile**. **Implication:** ship **YOLOv8n** (matches the ultralytics+Roboflow workflow); **demote SSDLite-MobileDet to fallback** (only if the *trained* 3-class model's on-Pi latency disappoints — its head is smaller than stock 80-class, so expect an equal/cleaner compile). **Still UNVERIFIED:** real Pi-4 + USB-Coral latency/FPS (op-map ≠ speed) — measure on-device (USB3 port; `libedgetpu1-std`). Toolchain versions: onnxruntime 1.27.0 (cp312), TF auto-pulled `<=2.19.0`. Files: this log, `mem:decisions/detector_build_plan`, `IMPLEMENTATION_PLAN §9.2`; (Strix) `~/turret-ml/yolov8n_saved_model/`.
- **2026-06-29 — Dataset finalized (single-class) + staged on Strix; training config locked.** Owner's Roboflow export is **single class `['bird']`** (`jayson-x-an0sg/pigeons-single-class`, **1152 train / 196 val / 63 test**, 517 MB, YOLO normalized xywh) — corrects my earlier 3-class assumption (`config.detector.num_classes=1`, output `[1,5,8400]`, matches `decode_v8` + golden test). **rsync'd to Strix** `~/turret-ml/datasets/pigeons-single-class/` with a **rewritten data.yaml** (absolute `path:` — Roboflow's `../train/images` resolves off-tree), ready for INT8 export calibration (the 1152 imgs satisfy the >300 calib gate). **Training config verified** for the owner's Ultralytics-HUB cloud run: base `yolov8n`, **`imgsz=256` (changed from proposed 640** — deploy is Coral-locked at 256, so train==deploy; 640 would overstate val mAP and its small-bird gain is lost at 256 inference), **`epochs≈300 / patience≈50`** (changed from 1000/100 — 1152 imgs converge early), rest of the proposed defaults kept (mosaic/fliplr/hsv/close_mosaic fine). Cloud is fine for the `.pt`; **export+compile stays on Strix** (compiler x86-local; dataset now there). Updated `mem:decisions/detector_build_plan`, `IMPLEMENTATION_PLAN §9.1`. Files: this log; (Strix) `~/turret-ml/datasets/pigeons-single-class/`.
- **2026-06-29 — First finetuned model converted + committed `models/` folder; deploy framing corrected.** Owner trained the first model on Ultralytics HUB (`test-8n-run-1.pt`, single-class bird) and provided the `.pt`. Pushed it to Strix, exported `format=edgetpu imgsz=256 int8=True data=pigeons-single-class/data.yaml nms=False dynamic=False` → **compile gate PASS: 1 subgraph, 252 ops all on TPU, on-chip 3.11 MiB / 3.68 free, off-chip 7.88 KiB** (smaller head than stock 80-class's 256 ops, as predicted). Created the **committed `models/`** folder (no gitignore exclusions) with: `bird_yolov8n_256_int8_edgetpu_run1.tflite` (3.4 MB, deploy candidate), `bird_yolov8n_256_run1.pt` (source), `yolov8n_coco80_256_int8_edgetpu.tflite` (vanilla reference) + `models/README.md` (naming convention, per-model gate numbers, how to add run<N>). **Deploy-framing corrected (owner): `~/pi-turret` is NOT stale** — push-to-deploy via the confirmed `pi`/`strix` remotes (→ `~/pi-turret`) is the active mechanism (Strix verified `6884880`; Pi maintained via `git push pi main`, unreachable at check). Removed the "stale/YoloRunner" narrative from CLAUDE.md, IMPLEMENTATION_PLAN §8, NEXT_SESSION_PROMPT.md, `mem:project/dev_environment`; reframed as **commit → push to pi/strix/origin; boxes only get committed+pushed code**. Next: golden fixture → deploy run1 to Pi → measure latency/FPS. Files: `models/*`, `models/README.md`, `.claude/CLAUDE.md`, `IMPLEMENTATION_PLAN.md` (§8/§9), `NEXT_SESSION_PROMPT.md`, this log, mems `dev_environment`+`detector_build_plan`.
